require 'spec_helper'

describe PoliticalNews do
  it 'has a version number' do
    expect(PoliticalNews::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
